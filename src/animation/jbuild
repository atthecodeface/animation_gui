(jbuild_version 1)

(executable (
  (name animation)
  (libraries (
    sdl_ogl_gui
    ogl_gui
    shm_ipc
    imagelib
    batteries
  ))
))

(alias (
  (name    run_top)
  (deps    (animation.exe))
  (action  (run ${<}))
))
